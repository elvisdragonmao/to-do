{
	"name": "sprintly",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"build": "pnpm --filter @sprintly/shared build && pnpm --filter @sprintly/web build && pnpm --filter @sprintly/api build",
		"dev": "pnpm --filter @sprintly/shared build && concurrently -k -n API,WEB -c blue,magenta \"pnpm --filter @sprintly/api dev\" \"pnpm --filter @sprintly/web dev\"",
		"format": "prettier --write .",
		"format:check": "prettier --check .",
		"start": "pnpm --filter @sprintly/api start",
		"test": "pnpm -r --if-present test",
		"typecheck": "pnpm -r --if-present typecheck"
	},
	"devDependencies": {
		"concurrently": "^10.0.0",
		"prettier": "^3.9.6",
		"prettier-plugin-astro": "^0.14.1",
		"prettier-plugin-organize-imports": "^4.3.0",
		"prettier-plugin-packagejson": "^3.0.2",
		"typescript": "^7.0.2"
	},
	"packageManager": "pnpm@11.21.0",
	"engines": {
		"node": ">=24"
	}
}
